using InvestmentForecast.Api.Controllers;
using InvestmentForecast.Api.Models.Request;
using InvestmentForecast.E2E.Tests.TestBuilder;
using Microsoft.AspNetCore.Mvc;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace InvestmentForecast.Api.Tests
{
    [TestClass]
    public class InvestmentForecastControllerTests
    {
  /*      private InvestmentForecastController _investmentForecastController;

        [TestInitialize]
        public void TestSetup()
        {
            _investmentForecastController = new InvestmentForecastController();
        }

        [TestMethod]
        public void Calculate_WhenLowRisk_ThenError()
        {
            //Arrange
            ForecastRequestViewModel request = new ForecastRequestViewModelBuilder()
                            .WithValidDefaults()
                            .WithInvestmentTerm(-1)
                            .Build();

            //Act
          //  IActionResult actual = _investmentForecastController.Calculate(request);

            //Assert
         //   Assert.AreEqual(0, actual);
         //   Assert.IsInstanceOfType(actual, typeof(BadRequestResult));
        }*/
    }
}
